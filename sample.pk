module sample;

int my_int = 1;
float wow = 1.0;
int triangle = 0;

{
    int blocky = 2;
    blocky + 5;
}

blocky + 5;

int my_func() {
    if (wow > 2.0) {
        return triangle;
    }

    while(my_int < 99) {
        triangle = triangle + my_int;
        my_int++;
        if (triangle > 15) {
            return triangle;
        }
    }

    return 0;
}

my_func();

triangle;